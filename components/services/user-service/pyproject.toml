[tool.poetry]
name = "user-service"
version = "0.1.0"
description = ""
authors = []
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pika = "^1.3.2"
peewee = "^3.17.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
